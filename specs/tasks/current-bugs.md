# Current Bugs

> Priority: P0 (Critical) → P3 (Low)

## P0 — CI Lint Failure

**Description:** GitHub Actions lint step fails with compilation errors.

**Root cause:** Missing `javax.inject` dependency in `:core:domain` module (resolved in c44e25d but CI may still fail).

**Dependencies:** None  
**Acceptance criteria:** `./gradlew lint` passes on CI.

---

## P0 — storage:datastore Compilation Error

**Description:** `SettingsSerializer.kt` cannot resolve `AppSettings` import.

**Root cause:** Missing `:core:domain` project dependency in `:storage:datastore/build.gradle.kts` (resolved in 6b1cfa9).

**Dependencies:** None  
**Acceptance criteria:** Module compiles on clean build.

---

## P1 — AndroidManifest mipmap missing

**Description:** App references `@mipmap/ic_launcher` but no launcher icon is included in resources.

**Impact:** App may use default Android icon or crash on some launchers.

**Fix:** Add a simple vector launcher icon in `res/drawable/` and update manifest.

---

## P1 — Missing ic_launcher resource in app/build

**Description:** The app module declares `android:icon="@mipmap/ic_launcher"` but no launcher icon resource is provided.

**Acceptance criteria:** App has a proper launcher icon.

---

## P1 — NovaApplication references BuildConfig but may not have it

**Description:** `NovaApplication.kt` uses `BuildConfig.DEBUG` which is generated by AGP. With `buildConfig = true` set, this should work, but verify on CI.

**Acceptance criteria:** App compiles without BuildConfig errors.
